// themes - ['subscription',]

mixin subscription-text-field(attr={})
    form(class=`text-field-form`)&attributes(attr)
        input(class=attr._class + ` text-field__input text-field_${attr.theme}`)&attributes(attr)